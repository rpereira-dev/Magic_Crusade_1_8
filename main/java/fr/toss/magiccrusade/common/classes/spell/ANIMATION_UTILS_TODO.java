package fr.toss.magiccrusade.common.classes.spell;

import net.minecraft.client.Minecraft;
import net.minecraft.entity.Entity;
import net.minecraft.util.EnumParticleTypes;
import net.minecraft.util.MathHelper;
import fr.toss.magiccrusade.client.entity.EntityFX_Colored;

public class ANIMATION_UTILS_TODO {
/* NODA DE FEU MAGE
	@Override
	public void animate(Entity caster, Entity target)
	{
		float	rayon;
		float	v_x;
		float	v_y;
		float	v_z;
		
		rayon = 4.0f;
		for (int phi = -180; phi < 180; phi++)
		{
			for (int teta = -90; teta < 90; teta++)
			{
				if (caster.worldObj.rand.nextInt() % 20 == 0)
				{
					v_x = rayon * MathHelper.cos(teta) * MathHelper.cos(phi);
					v_y = rayon * MathHelper.cos(teta) * MathHelper.sin(phi);
					v_z = rayon * MathHelper.sin(teta);
					caster.worldObj.spawnParticle(EnumParticleTypes.FLAME, caster.posX, caster.posY, caster.posZ, v_x, v_y, v_z, 1);
				}
			}
		}
	}*/
}